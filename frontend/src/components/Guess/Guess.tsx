import './Guess.css';

interface GuessProps {
    code: string;
}

const Guess = (props: GuessProps) => {
    return (
        <div>
            <h1>Guess</h1>
        </div>
    )
}

export default Guess;